# SelfHost Atlas — install-verification dataset

An open dataset of self-hostable applications whose published install
instructions have been **run on a real, fresh server** — recording which OS the
run happened on and when.

- **Files:** `install-verification.json`, `install-verification.csv` (same
  records, same field order)
- **Licence:** [CC BY 4.0](./LICENSE)
- **Methodology:** <https://selfhostatlas.com/how-we-test>
- **Source:** <https://selfhostatlas.com>
- **Generated by:** `npm run data:export` (`scripts/export-dataset.ts`)

---

## What is measured, and what is not

This is the part that matters. The dataset deliberately contains **only** what
the project actually observed, plus values that are clearly labelled as
documented rather than observed.

| Status | Fields | Meaning |
|---|---|---|
| **Measured** | `install_verified_os`, `install_verified_date` | A real install run happened: the app's published install snippet was executed on a clean instance of that OS, on that date, and reached a working state. |
| **Documented, not measured** | `documented_min_ram_mb` | The minimum RAM the upstream project documents, or SelfHost Atlas's sizing recommendation where upstream documents none. Values sit on standard instance tiers (256/512/1024/2048…). **This is a recommendation, not an observation.** Do not cite it as measured memory usage. |
| **Catalog metadata** | `slug`, `title`, `category`, `license`, `repo_url`, `website_url`, `is_docker_ready`, `content_updated` | Facts about the project and about our page, not measurements of a running system. |
| **Not present** | measured RAM/CPU usage, setup time, maintenance time | These are **not yet measured**, so they are not in the export at all — not even as null columns. They will appear only once real measurements exist. |

`scripts/export-dataset.ts` refuses to publish a field listed as measured if
that column is empty in the database, and `tests/export-dataset.test.ts` fails
if an empty column is ever added to the export. That guard exists because
publishing a documented value as a measured one would be a false claim.

## Field reference

| Field | Type | Nullable | Description |
|---|---|---|---|
| `slug` | string | no | Stable identifier. Page: `https://selfhostatlas.com/self-host/<slug>` |
| `title` | string | no | Project name as commonly written |
| `category` | string | yes | Category slug (e.g. `photos`, `password-managers`) |
| `license` | string | no | SPDX-style short identifier (e.g. `AGPL-3.0`, `MIT`) |
| `repo_url` | string | yes | Upstream source repository |
| `website_url` | string | yes | Upstream project website |
| `is_docker_ready` | boolean | no | Upstream publishes an official container image or Compose file |
| `documented_min_ram_mb` | integer | no | Documented/recommended minimum RAM in MB — **see the table above** |
| `install_verified_os` | string | yes | OS the install run happened on (e.g. `Ubuntu 26.04`). Null = not yet verified |
| `install_verified_date` | date (`YYYY-MM-DD`) | yes | Date of that run. Null = not yet verified |
| `content_updated` | date (`YYYY-MM-DD`) | yes | Date the SelfHost Atlas page for this app was last editorially reviewed |

Judge freshness from `install_verified_date`: an install verified a year ago
says little about today's upstream instructions. Every record carries its own
date precisely so you can filter on it rather than trust the dataset as a whole.

Records with a null `install_verified_os` / `install_verified_date` are included
so the catalog is complete and the unverified share is visible. Filter them out
if you only want verified rows.

## JSON envelope

```json
{
  "schema_version": "1.0.0",
  "generated_at": "2026-09-10T00:00:00.000Z",
  "license": "CC-BY-4.0",
  "attribution": "SelfHost Atlas — https://selfhostatlas.com",
  "methodology": "https://selfhostatlas.com/how-we-test",
  "record_count": 106,
  "records": [ /* … sorted by slug … */ ]
}
```

`schema_version` follows semver: a **major** bump means a field was removed or
its meaning changed; a **minor** bump means a field was added. Records are
always sorted by `slug`, so a re-export diffs cleanly.

The CSV carries the record fields only, with a header row, RFC 4180 quoting and
the same field order. It has no envelope — take `generated_at` and
`schema_version` from the JSON.

## Update cadence

Regenerated when the catalog changes — in practice after each install-sweep run
and each content wave, roughly monthly. `generated_at` in the JSON is the
authoritative export timestamp; the per-record `install_verified_date` is what
tells you how old any individual observation is.

## How to attribute

Required by CC BY 4.0. Any of these satisfies it:

**Prose**

> Install-verification data by [SelfHost Atlas](https://selfhostatlas.com),
> licensed under [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/).

**Markdown**

```markdown
Data: [SelfHost Atlas install-verification dataset](https://selfhostatlas.com) ([CC BY 4.0](https://creativecommons.org/licenses/by/4.0/))
```

**HTML**

```html
<p>Data: <a href="https://selfhostatlas.com">SelfHost Atlas</a>,
<a href="https://creativecommons.org/licenses/by/4.0/">CC BY 4.0</a></p>
```

If you modified the data, say so ("adapted from…"). A link back to
<https://selfhostatlas.com> is the credit; there is no other condition.

## Corrections

If a record is wrong — an install that no longer works, a licence change, a
dead repo URL — open an issue or email <hello@selfhostatlas.com>. Corrections
land in the catalog and appear in the next export.

---

*These files are generated from the SelfHost Atlas catalog, not hand-edited.
Corrections belong in the catalog — open an issue here and the fix appears in
the next export.*
